# Copy to .env.prod and fill in real values — never commit .env.prod itself.
#   cp .env.prod.example .env.prod

DJANGO_SETTINGS_MODULE=blogermenia.settings

# --- Django core ---
# Generate a fresh key: python -c "import secrets;print(secrets.token_urlsafe(50))"
DJANGO_SECRET_KEY=change-me-to-a-long-random-value
DJANGO_DEBUG=False
DJANGO_ALLOWED_HOSTS=yourdomain.com
DJANGO_CSRF_TRUSTED_ORIGINS=https://yourdomain.com
# Flip to True only once nginx (or a load balancer in front of it) terminates TLS.
DJANGO_SECURE_SSL_REDIRECT=False

# --- Database ---
# SQLite — no separate DB container needed. docker-compose.prod.yml sets
# SQLITE_PATH itself (points at a named volume), you don't need to set it here.

# --- Celery / Redis ---
# "redis" is the service name on the compose network — every container
# resolves it automatically, no manual host/IP config needed.
CELERY_BROKER_URL=redis://redis:6379/2
CELERY_RESULT_BACKEND=redis://redis:6379/3

# --- Gunicorn ---
GUNICORN_WORKERS=1
GUNICORN_THREADS=2

# --- Google Gemini (embeddings + text generation) ---
GOOGLE_API_KEY=
GEMINI_EMBEDDING_MODEL=gemini-embedding-001
GEMINI_MODEL=gemini-3.5-flash
